# Persona 1: copiar a .env.local y completar con los valores del proyecto Supabase.
# NEXT_PUBLIC_* queda expuesto al cliente a propósito (anon key es segura por RLS).
# SUPABASE_SERVICE_ROLE_KEY NUNCA debe usarse fuera de un Route Handler (app/api/.../route.ts).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
